<%= form_with(model: post, local: true) do |form| %>
  <div class="row">
    <%= form.text_field :word, class: "form-control col-9" %>
    <%= form.submit "送信", class: "btn btn-primary col-3" %>
  </div>
<% end %>
